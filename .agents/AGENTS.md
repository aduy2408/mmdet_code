1. If need to run, and there is no .venv insdie workspace, use conda activate ml2, source /home/duylearch/miniconda3/bin/activate ml2
2. When asked about codebase architecture, dependencies, or file relationships, first run /understand to generate or refresh the knowledge graph, then use it to answer.
3. Use /ponytail at the start of each session
4. Ask if there is a .venv in the current working directory first before running tests(because ml2 might not cover everything)
